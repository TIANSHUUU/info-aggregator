name: 每日数据更新 + 部署

on:
  workflow_dispatch:        # 手动触发（gh workflow run / Actions 页面 / 站内「立即更新」按钮）
  # 已移除每日 schedule 定时抓取：抓取常失败会持续发送 GitHub Actions 失败邮件，
  # 且站内已有「立即更新」按钮可随时手动触发。如需恢复每日自动，加回下面两行即可：
  # schedule:
  #   - cron: '41 3 * * *'    # 每天 03:41 UTC ≈ 墨尔本 13:41（AEST；夏令时 AEDT 为 14:41）

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  fetch-build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: scripts/requirements.txt

      - name: Install Python dependencies
        run: pip install -r scripts/requirements.txt

      - name: Fetch all sources
        run: python scripts/fetch_all.py
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Node dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
